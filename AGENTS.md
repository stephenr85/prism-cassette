> You are in **rushing/prism-cassette** — deterministic Prism record/replay for tests and application code.

A standalone PHP Composer package (Laravel service provider) that records and replays `prism-php/prism` LLM calls, so tests and application code can run deterministically against fixture "cassettes" instead of live model calls.
